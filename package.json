{
  "name": "contractid-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "ContractID — onchain identity + scoring for smart contracts (api, web, extension, sdk)",
  "type": "module",
  "scripts": {
    "dev:api": "pnpm --filter @contractid/api dev",
    "dev:web": "pnpm --filter @contractid/web dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "postinstall": "node scripts/patch-enscribe.mjs"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20"
  }
}
